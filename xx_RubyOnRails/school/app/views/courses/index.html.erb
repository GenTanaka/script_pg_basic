<h1>Courses</h1>

<table>
    <tbody>
        <% @courses.each do |course| %>
            <tr>
                <td><%= course.name %></td>
                <td><%= link_to 'Show', course %></td>
            </tr>
        <% end %>
    </tbody>
</table>